
export interface Address {
    id: string;
    street: string;
    number: string;
    cityId: string;
    cityName: string;
}

export interface Country {
    id: string;
    name: string;
}

export interface County {
    id: string;
    name: string;
}

export interface City {
    id: string;
    name: string;
}